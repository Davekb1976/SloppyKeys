# assets/stages/expedition

Expedition's stage cards, one per map, named `<map slug>.png`.

Its own folder rather than sharing Story's: Expedition offers a subset of the same maps, but
its cards look different on screen, so a Story crop will not match here.

No list here on purpose — the Image Manager's **Stage Selection** section derives it from
`GAMEMODES["Expedition"].maps`.
